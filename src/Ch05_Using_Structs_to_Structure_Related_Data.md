# 使用结构体组织相关数据

所谓结构体，`struct`，或 *structure*，属于一种自定义数据类型，允许咱们将多个构成有意义分组的相关值打包在一起并命名。若咱们熟悉某门面向对象的语言，那么结构体就像对象的数据属性。在这一章中，我们将比对元组与结构体，以在咱们已知内容基础上构建（新知识），并说明结构体何时是更好的数据分组方式。

我们将演示如何定义与实例化结构体。我们将讨论如何定义关联函数，尤其是称为 *方法，method* 的关联函数，以指定出某种结构体类型关联的行为。结构体和枚举（会于第 6 章中讨论）是在咱们程序域中创建新类型的构件，以充分利用 Rust 的编译时类型检查。
